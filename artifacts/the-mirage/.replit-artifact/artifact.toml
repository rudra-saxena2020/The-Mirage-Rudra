kind = "web"
previewPath = "/"
title = "The Mirage"
version = "1.0.0"
id = "artifacts/the-mirage"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20363

[services.development]
run = "pnpm --filter @workspace/the-mirage run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/the-mirage", "run", "build" ]
publicDir = "artifacts/the-mirage/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20363"
BASE_PATH = "/"
